-- Defining the gun
local gun = script.Parent
-- Definign the gun sounds
local gun_shot = gun.Handle['Gun Shot']

gun.Equipped:Connect(function(mouse)
	mouse.Button1Down:Connect(function()
		gun_shot:Play()
	end)
end)
